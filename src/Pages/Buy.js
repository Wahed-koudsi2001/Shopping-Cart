import ShopBlock from "../Components/ShopBlock/ShopBlock"

export default function Buy({ setCountCart }) {
    return (
        <div>
            <ShopBlock setCountCart={setCountCart} />
        </div>
    )
}
